<?xml version="1.0" encoding="utf-8"?>
<!--
  XML themes only exist to bridge the splash + system bars while Compose
  takes over. Once `setContent { VctsTheme { ... } }` runs, the actual UI
  styling comes from Compose tokens (ui/theme/*).
-->
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Vcts.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">350</item>
        <item name="postSplashScreenTheme">@style/Theme.Vcts</item>
    </style>

    <!--
      Bridge theme used between the splash and Compose drawing the first
      frame. We extend AppCompat (already on the classpath) instead of the
      View-based Material Components theme so we don't pull in the
      `com.google.android.material:material` AAR for a Compose-only app.
      Compose's MaterialTheme owns all real styling - this theme just sets
      transparent system bars and a day/night-aware backdrop.
    -->
    <style name="Theme.Vcts" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>

</resources>
